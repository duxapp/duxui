import { PickerView as OldPickerView, PickerViewColumn as OldPickerViewColumn } from '@tarojs/components'

export const PickerView = props => {
  return <OldPickerView {...props} />
}

export const PickerViewColumn = props => {
  return <OldPickerViewColumn {...props} />
}
